import tkinter as tk

def lmfao():
            os.system("msg * operation failed")

root = tk.Tk()
root.title("your computer has virus")
open_button = tk.Button(root, text="delete virus", command=open_code_window)
open_button.pack(pady=10)
root.mainloop()
